import { Component, OnInit } from '@angular/core';

@Component({
    selector: 'app-header',
    templateUrl: 'header.temp.html',
    styleUrls: ['header.scss'],
})

export class Header {
    constructor() {
        
    }
}