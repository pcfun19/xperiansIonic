import { ElementRef, EventEmitter, OnDestroy, OnInit, Renderer2 } from '@angular/core';
import { SWSwipeCardConfig } from './swipe-card.config';
export declare class SwipeableDirective implements OnInit, OnDestroy {
    private _defaultConfig;
    protected el: ElementRef;
    renderer: Renderer2;
    private readonly animationDuration;
    private isReleased;
    private element;
    private overlay;
    private releaseRadius;
    private timeoutId;
    width: number;
    height: number;
    fixed: Boolean;
    orientation: string;
    released: EventEmitter<{}>;
    swiped: EventEmitter<boolean>;
    className: boolean;
    onPan(event: any): void;
    onPanEnd(event: any): void;
    readonly allowSwipe: boolean;
    constructor(_defaultConfig: SWSwipeCardConfig, el: ElementRef, renderer: Renderer2);
    ngOnDestroy(): void;
    ngOnInit(): void;
    private initializeOverlay();
    private translate({x, y, rotate, duration});
    private handleSwipeChange(event);
    private handleCancelSwipe();
    private destroy();
    private setBackgroundOverlay(like, opacity);
    private resetBackgroundOverlay();
    private resetElementStyles();
    private isSwipePositive(event);
    private removeOverlay(like);
    private handleSwipeEnd(like);
    simulateSwipe(like: boolean): void;
}
